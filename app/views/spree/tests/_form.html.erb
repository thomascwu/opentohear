<%= form_with(model: spree_test, local: true) do |form| %>
  <% if spree_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spree_test.errors.count, "error") %> prohibited this spree_test from being saved:</h2>

      <ul>
      <% spree_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
