<%= form_with(model: spree_hear, local: true) do |form| %>
  <% if spree_hear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spree_hear.errors.count, "error") %> prohibited this spree_hear from being saved:</h2>

      <ul>
      <% spree_hear.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
