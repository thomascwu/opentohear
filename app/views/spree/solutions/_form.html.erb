<%= form_with(model: spree_solution, local: true) do |form| %>
  <% if spree_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spree_solution.errors.count, "error") %> prohibited this spree_solution from being saved:</h2>

      <ul>
      <% spree_solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
